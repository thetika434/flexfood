name: backend
description: Backend FlexFood — serveur REST Dart + Prisma + PostgreSQL
version: 1.0.0

environment:
  sdk: ^3.9.2

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  dart_jsonwebtoken: ^2.14.1
  bcrypt: ^1.1.3
  orm: ^5.3.4
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.3
  uuid: ^4.5.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
